# multi_agent_system
A multi-agent AI coding assistant with Planner, Coder, and Debugger agents. The system dynamically generates execution plans, writes code, detects language (Java/Python), executes it, and iteratively debugs errors. Integrated both OpenAI APIs and local LLM (Ollama) with runtime switching and memory-based context handling.
